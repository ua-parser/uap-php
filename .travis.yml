language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly
  - hhvm

matrix:
    allow_failures:
        - php: 5.3
        - php: hhvm
        - php: nightly

before_script:
  # Remove XDebug.
  - if [ ! "$TRAVIS_PHP_VERSION" == "nightly" ]; then phpenv config-rm xdebug.ini; fi

install:
  - composer install
  - bin/uaparser ua-parser:update || true

script:
  - ./vendor/bin/phpunit

sudo: false
